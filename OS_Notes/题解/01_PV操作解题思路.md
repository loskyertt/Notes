
思路框架（通用方法）：

1. **识别共享变量**
    - 找出两个进程都访问的变量 → 这些是可能产生冲突的点。
2. **分析执行顺序的不确定性**
    - 并发执行意味着指令可以以任意顺序交错执行。
3. **寻找“不一致”的执行路径**
    - 尝试构造一种执行顺序，使得结果不符合预期（比如逻辑错误、数据错乱）。
4. **判断是否存在竞态条件（Race Condition）**
    - 如果结果依赖于执行顺序，则存在竞态条件 → 不安全。
5. **提出修改方案**
    - 使用互斥机制（如锁、信号量）保护共享变量。